# Шпаргалка по Vim, Git и Bash

## 📝 VIM - Текстовый редактор

### Режимы работы
Vim имеет несколько режимов. Главное понять: в каком режиме ты находишься!

| Режим | Описание | Как войти |
|-------|----------|-----------|
| **Normal** | Режим навигации и команд (по умолчанию) | `Esc` |
| **Insert** | Режим редактирования текста | `i` |
| **Visual** | Режим выделения текста | `v` |
| **Command** | Режим команд | `:` |

### Основные команды

#### Редактирование
```
i          - вставка (Insert) - начать редактирование ДО курсора
a          - append - начать редактирование ПОСЛЕ курсора
o          - открыть новую строку НИЖЕ и войти в режим вставки
O          - открыть новую строку ВЫШЕ и войти в режим вставки
Esc        - выйти из режима редактирования в Normal mode
```

#### Навигация (в Normal mode)
```
h          - влево (←)
j          - вниз (↓)
k          - вверх (↑)
l          - вправо (→)

w          - перейти к следующему слову
b          - перейти к предыдущему слову
0          - в начало строки
$          - в конец строки
gg         - в начало файла
G          - в конец файла
```

#### Удаление (в Normal mode)
```
x          - удалить символ под курсором
dd         - удалить всю строку
dw         - удалить слово
D          - удалить до конца строки
```

#### Копирование и вставка (в Normal mode)
```
yy         - скопировать строку (yank)
yw         - скопировать слово
p          - вставить после курсора (paste)
P          - вставить перед курсором
```

#### Отмена и повтор
```
u          - отменить последнее действие (undo)
Ctrl+r     - вернуть отмененное (redo)
```

#### Сохранение и выход (из Normal mode, начинаются с :)
```
:w         - сохранить файл (write)
:q         - выйти (quit)
:wq        - сохранить и выйти
:q!        - выйти БЕЗ сохранения (force quit)
ZZ         - быстрый способ сохранить и выйти
```

#### Поиск (в Normal mode)
```
/текст     - найти "текст" вперед
?текст     - найти "текст" назад
n          - следующее совпадение
N          - предыдущее совпадение
```

### Аналогия
**Vim как швейцарский нож:** Сначала кажется сложным, но когда освоишь - очень мощный инструмент!

---

## 🔀 GIT - Система контроля версий

### Концепция работы
```
Working Directory → Staging Area → Repository
   (рабочая папка)   (подготовка)   (история)
```

**Аналогия:** Пишешь книгу (Working) → Собираешь главы для публикации (Staging) → Публикуешь книгу (Commit)

### Настройка Git (один раз)
```bash
git config --global user.name "Твое Имя"
git config --global user.email "email@example.com"
git config --global init.defaultBranch main
```

### Создание репозитория
```bash
git init                    # Создать новый репозиторий в текущей папке
git clone <url>             # Скопировать существующий репозиторий
```

### Основные команды

#### Проверка статуса
```bash
git status                  # Показать статус: что изменилось, что готово к коммиту
git log                     # Показать историю коммитов
git log --oneline           # Компактная история (по одной строке)
git log --graph             # История с графом веток
git diff                    # Показать изменения (что изменилось в файлах)
```

#### Добавление файлов в staging
```bash
git add <файл>              # Добавить конкретный файл
git add .                   # Добавить ВСЕ измененные файлы
git add *.js                # Добавить все файлы .js
git add -p                  # Добавить изменения по частям (интерактивно)
```

#### Коммит (сохранение в историю)
```bash
git commit -m "Сообщение"   # Коммит с сообщением
git commit                  # Открыть vim для написания сообщения
git commit --amend          # Изменить последний коммит (осторожно!)
```

**Правила хорошего commit message:**
- Первая строка - краткое описание (до 50 символов)
- Используй повелительное наклонение: "Add feature" не "Added feature"
- Примеры: "Add login form", "Fix memory leak", "Update README"

#### Ветки (branches)
```bash
git branch                  # Показать все ветки
git branch <имя>            # Создать новую ветку
git branch -d <имя>         # Удалить ветку
git checkout <ветка>        # Переключиться на ветку
git checkout -b <ветка>     # Создать и переключиться на новую ветку
git switch <ветка>          # Современный способ переключения
git merge <ветка>           # Слить ветку в текущую
```

**Аналогия веток:** Главная книга (main) и черновики (feature branches). Когда черновик готов - включаешь его в основную книгу (merge).

#### Отмена изменений
```bash
git restore <файл>          # Отменить изменения в файле (вернуть к последнему коммиту)
git restore --staged <файл> # Убрать файл из staging (но сохранить изменения)
git reset HEAD~1            # Отменить последний коммит (сохранить изменения)
git reset --hard HEAD~1     # Отменить последний коммит (УДАЛИТЬ изменения) ⚠️
```

#### Работа с удаленным репозиторием (GitHub, GitLab)
```bash
git remote add origin <url> # Привязать удаленный репозиторий
git remote -v               # Показать удаленные репозитории
git push origin main        # Отправить изменения на сервер
git pull origin main        # Скачать изменения с сервера
git fetch                   # Скачать информацию о изменениях (без слияния)
```

#### Полезные команды
```bash
git stash                   # Спрятать изменения (временно)
git stash pop               # Вернуть спрятанные изменения
git tag v1.0.0              # Поставить тег (версию)
git show <commit-id>        # Показать детали коммита
```

---

## 💻 BASH - Командная строка

### Навигация
```bash
pwd                         # Print Working Directory - где я сейчас
ls                          # Список файлов в текущей папке
ls -la                      # Список всех файлов (включая скрытые) с деталями
cd <папка>                  # Change Directory - перейти в папку
cd ..                       # Перейти на уровень выше
cd ~                        # Перейти в домашнюю папку
cd -                        # Вернуться в предыдущую папку
```

### Работа с файлами и папками
```bash
mkdir <папка>               # Создать папку
mkdir -p путь/к/папке       # Создать вложенные папки
touch <файл>                # Создать пустой файл
rm <файл>                   # Удалить файл
rm -r <папка>               # Удалить папку рекурсивно
rm -rf <папка>              # Удалить папку силой (осторожно!) ⚠️
cp <откуда> <куда>          # Копировать файл
cp -r <откуда> <куда>       # Копировать папку
mv <откуда> <куда>          # Переместить/переименовать
```

### Просмотр файлов
```bash
cat <файл>                  # Показать содержимое файла
less <файл>                 # Просмотр файла (постранично)
head <файл>                 # Первые 10 строк
head -n 20 <файл>           # Первые 20 строк
tail <файл>                 # Последние 10 строк
tail -f <файл>              # Следить за файлом в реальном времени
```

### Поиск
```bash
find . -name "*.js"         # Найти все .js файлы
find . -type f -name "test*" # Найти файлы начинающиеся с "test"
grep "текст" <файл>         # Найти текст в файле
grep -r "текст" .           # Найти текст во всех файлах рекурсивно
grep -i "текст" <файл>      # Поиск без учета регистра
```

### Права доступа
```bash
chmod +x <файл>             # Сделать файл исполняемым
chmod 755 <файл>            # Установить права rwxr-xr-x
chown user:group <файл>     # Изменить владельца файла
```

### Процессы
```bash
ps aux                      # Показать все процессы
top                         # Монитор процессов в реальном времени
htop                        # Улучшенный монитор (если установлен)
kill <PID>                  # Остановить процесс
killall <имя>               # Остановить все процессы с именем
```

### Сеть
```bash
ping google.com             # Проверить соединение
curl <url>                  # Скачать содержимое URL
wget <url>                  # Скачать файл
ifconfig                    # Показать сетевые интерфейсы (macOS)
ip addr                     # Показать сетевые интерфейсы (Linux)
```

### Переменные окружения
```bash
echo $PATH                  # Показать переменную PATH
export VAR=value            # Установить переменную
env                         # Показать все переменные окружения
```

### Комбинирование команд
```bash
команда1 && команда2        # Выполнить команду2 ТОЛЬКО если команда1 успешна
команда1 || команда2        # Выполнить команда2 ТОЛЬКО если команда1 провалилась
команда1 ; команда2         # Выполнить обе команды последовательно
команда1 | команда2         # Pipe - передать вывод команды1 в команду2
команда > файл              # Перенаправить вывод в файл (перезаписать)
команда >> файл             # Добавить вывод в конец файла
```

### Полезные сочетания клавиш
```bash
Ctrl+C                      # Прервать текущую команду
Ctrl+D                      # Выход (EOF)
Ctrl+Z                      # Приостановить процесс
Ctrl+R                      # Поиск по истории команд
Ctrl+A                      # В начало строки
Ctrl+E                      # В конец строки
Ctrl+U                      # Удалить до начала строки
Ctrl+K                      # Удалить до конца строки
Tab                         # Автодополнение
↑/↓                         # Предыдущая/следующая команда из истории
```

### NPM команды (Node Package Manager)
```bash
npm init                    # Создать package.json
npm init -y                 # Создать package.json с настройками по умолчанию
npm install <пакет>         # Установить пакет локально
npm install -g <пакет>      # Установить пакет глобально
npm install                 # Установить все зависимости из package.json
npm uninstall <пакет>       # Удалить пакет
npm update                  # Обновить пакеты
npm list                    # Показать установленные пакеты
npm run <скрипт>            # Запустить скрипт из package.json
npm start                   # Запустить приложение
npm test                    # Запустить тесты
```

---

## 🎯 Типичные рабочие процессы

### Начало работы с проектом
```bash
mkdir my-project            # Создать папку
cd my-project               # Перейти в папку
git init                    # Инициализировать Git
npm init -y                 # Инициализировать Node.js проект
touch .gitignore            # Создать .gitignore
```

### Ежедневная работа с Git
```bash
git status                  # Проверить что изменилось
git add .                   # Добавить все изменения
git commit                  # Сделать коммит (откроется vim)
# В vim: i -> написать сообщение -> Esc -> :wq
git push origin main        # Отправить на GitHub
```

### Работа с ветками
```bash
git checkout -b feature     # Создать ветку для новой функции
# ... работаем над функцией ...
git add .
git commit -m "Add new feature"
git checkout main           # Вернуться на main
git merge feature           # Влить изменения из feature
git branch -d feature       # Удалить ветку feature
```

### Установка и работа с пакетами
```bash
npm install express         # Установить Express
npm install --save-dev jest # Установить Jest для разработки
npm run dev                 # Запустить в режиме разработки
npm test                    # Запустить тесты
```

---

## 💡 Советы

### Git
- Коммить часто, маленькими порциями
- Писать понятные commit messages
- Не коммитить node_modules, .env файлы
- Создавать ветки для новых фич

### Vim
- Всегда знай в каком режиме находишься
- Esc - твой друг (возврат в Normal mode)
- Начни с основных команд: i, Esc, :wq, :q!
- Практикуйся! Со временем станет естественным

### Bash
- Используй Tab для автодополнения
- Используй ↑ для поиска предыдущих команд
- Будь осторожен с rm -rf
- Читай сообщения об ошибках - они часто подсказывают решение

---

## 📚 Дополнительные ресурсы

- **Vim Tutorial:** `vimtutor` (команда в терминале - интерактивный туториал!)
- **Git Docs:** https://git-scm.com/doc
- **Bash Guide:** https://www.gnu.org/software/bash/manual/

---

**Удачи в обучении! 🚀**
