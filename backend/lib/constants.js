const TASK_STATUSES = ['backlog', 'planned', 'in_progress', 'done', 'canceled', 'archived'];

const BOOKING_STATUSES = ['pending', 'confirmed', 'canceled', 'completed', 'no_show'];

module.exports = { TASK_STATUSES, BOOKING_STATUSES };
