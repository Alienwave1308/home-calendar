version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: home-calendar-db
    restart: unless-stopped
    environment:
      - POSTGRES_USER=calendar
      - POSTGRES_PASSWORD=calendar123
      - POSTGRES_DB=home_calendar
    volumes:
      - pgdata:/var/lib/postgresql/data

  app:
    build: .
    container_name: home-calendar
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://calendar:calendar123@db:5432/home_calendar
      - JWT_SECRET=${JWT_SECRET}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - ALLOW_PASSWORD_AUTH=${ALLOW_PASSWORD_AUTH:-false}
      - MASTER_TIMEZONE=${MASTER_TIMEZONE:-Asia/Novosibirsk}
      - MASTER_TELEGRAM_USER_ID=${MASTER_TELEGRAM_USER_ID}
      - MASTER_DISPLAY_NAME=${MASTER_DISPLAY_NAME}
      - VK_APP_SECRET=${VK_APP_SECRET}
      - VK_MINI_APP_SECRET=${VK_MINI_APP_SECRET}
      - VK_OAUTH_SCOPE=${VK_OAUTH_SCOPE}
      - VK_APP_ID=${VK_APP_ID}
      - VK_OAUTH_REDIRECT_URI=${VK_OAUTH_REDIRECT_URI}
      - VK_GROUP_TOKEN=${VK_GROUP_TOKEN}
      - VK_GROUP_ID=${VK_GROUP_ID}
      - VK_SECRET=${VK_SECRET}
      - VK_CONFIRMATION_STRING=${VK_CONFIRMATION_STRING}
      - MASTER_BOOKING_SLUG=${MASTER_BOOKING_SLUG}
      - APP_DOMAIN=${APP_DOMAIN}
      - TELEGRAM_BOT_USERNAME=${TELEGRAM_BOT_USERNAME:-Rova_Epil_Bot}
      - TELEGRAM_API_BASE=${TELEGRAM_API_BASE:-https://home-calendar.niggest137.workers.dev}
      - WEB_BOOKING_ENABLED=${WEB_BOOKING_ENABLED:-false}
      - WEB_BOOKING_ALLOWED_SLUGS=${WEB_BOOKING_ALLOWED_SLUGS}
    depends_on:
      - db

  caddy:
    image: caddy:2.10-alpine
    container_name: home-calendar-caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    environment:
      - APP_DOMAIN=${APP_DOMAIN}
      - LETSENCRYPT_EMAIL=${LETSENCRYPT_EMAIL}
    volumes:
      - ./deploy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - app

volumes:
  pgdata:
  caddy_data:
  caddy_config:
